x_train, y_train, x_test, y_test, subject_train and subject_test contain the data from uploaded tables.
all_train, all_test, and Test_Train merge the previous datasets into one unstructured set to extract measurements/standard deviation.
features contains the correct names for the all of the tables, which are applied to the column names stored in StandardMeans
